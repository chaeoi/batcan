# Select exactly one profile and leave the other two commented out.
# profile: kvms  # 心向阳 battery: DALY K-series 13S 100A, external KVMS CAN.
# profile: htbms # Zhenyuan HTBMS V1.1.0, 0x1822xxxx active broadcast.
# profile: jbd   # JBD CANBUS-compatible 0x100-0x110 RTR query protocol.

# interface: can5 # SocketCAN interface on this machine, such as can0 or can5.
# bitrate: 250000 # Optional physical-rate override; otherwise use profile default.
